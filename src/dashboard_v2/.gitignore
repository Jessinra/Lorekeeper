node_modules/
build/
.svelte-kit/
.env
.env.*
!.env.example


# Playwright
# NOTE: tests/visual-baseline/ is intentionally NOT ignored — the visual
# regression snapshots MUST be committed so CI has a baseline to diff against.
test-results/
playwright-report/
